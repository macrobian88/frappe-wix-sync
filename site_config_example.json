# Example site_config.json configuration for Wix integration

{
  "db_name": "your_site_db",
  "db_password": "your_db_password", 
  "encryption_key": "your_encryption_key",
  
  "wix_site_id": "your-wix-site-id-here",
  "wix_api_key": "your-wix-api-key-here",
  "wix_access_token": "your-wix-access-token-here"
}

# How to get Wix credentials:
# 1. Go to https://dev.wix.com/
# 2. Create a new app or select existing app
# 3. Enable "Wix Stores" API in the app settings
# 4. Get your API Key from the app dashboard
# 5. Generate an Access Token (OAuth2 flow or API Key method)
# 6. Find your Site ID in your Wix site dashboard URL or API

# Note: Never commit actual credentials to version control!
# This file is for reference only.
